{erl_opts, [
    debug_info,
    warnings_as_errors
]}.

{apps_dirs, [
    "proc",
    "gen_server"
]}.

{deps, []}.

{profiles, [
    {test, [
        {erl_opts, [
            debug_info
        ]}
    ]}
]}.
