{erl_opts, [debug_info]}.

% Указываем исходники приложений
{src_dirs, ["src/proc", "src/gen_server"]}.

% Пути к тестам
{test_dirs, ["src/proc/test", "src/gen_server/test"]}.

% Плагины
{project_plugins, [
    rebar3_lint,
    erlfmt,
    rebar3_proper
]}.

% Настройки erlfmt
{erlfmt, [
    {print_width, 90},
    {files, [
        "{src/proc,src/gen_server,src/proc/test,src/gen_server/test}/*.{erl,src}",
        "rebar.config"
    ]}
]}.

% EUnit опции
{eunit_opts, [verbose]}.
